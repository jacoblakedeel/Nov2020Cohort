
import React from 'react'

const Clear = (props) => {
    return (
        <>
            <button className="btn btn-warning" onClick={props.clear}>Clear</button>
        </>
    )
}

export default Clear;
